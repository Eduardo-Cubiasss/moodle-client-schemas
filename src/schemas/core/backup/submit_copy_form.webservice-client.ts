/** Handles ajax submission of course copy form. */
export interface CoreBackupSubmitCopyFormParams {
    /** The data from the create copy form, encoded as a json array */
    jsonformdata: string | null;
}

/** JSON response. */
export type CoreBackupSubmitCopyFormReturns = string | null;

export type CoreBackupSubmitCopyFormReturn = CoreBackupSubmitCopyFormReturns;
export type core_backup_submit_copy_form_returns = CoreBackupSubmitCopyFormReturns;
