/** Mark all conversation messages as read for a given user */
export interface CoreMessageMarkAllConversationMessagesAsReadParams {
    /** The user id who who we are marking the messages as read for */
    userid: number | null;
    /** The conversation id who who we are marking the messages as read for */
    conversationid: number | null;
}

export type CoreMessageMarkAllConversationMessagesAsReadReturns = unknown;

export type CoreMessageMarkAllConversationMessagesAsReadReturn = CoreMessageMarkAllConversationMessagesAsReadReturns;
export type core_message_mark_all_conversation_messages_as_read_returns = CoreMessageMarkAllConversationMessagesAsReadReturns;
