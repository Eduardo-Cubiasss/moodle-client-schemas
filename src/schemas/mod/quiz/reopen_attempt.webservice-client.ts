/** Re-open an attempt that is currently in the never submitted state. */
export interface ModQuizReopenAttemptParams {
    /** The id of the attempt to reopen */
    attemptid: number | null;
}

export type ModQuizReopenAttemptReturns = unknown;

export type ModQuizReopenAttemptReturn = ModQuizReopenAttemptReturns;
export type mod_quiz_reopen_attempt_returns = ModQuizReopenAttemptReturns;
