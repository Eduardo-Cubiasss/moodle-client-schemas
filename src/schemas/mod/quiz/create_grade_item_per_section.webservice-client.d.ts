/** For a quiz with no grade items yet, create a grade item for each section, with the questions in that section assigned. */
export interface ModQuizCreateGradeItemPerSectionParams {
    /** The quiz to update slots for. */
    quizid: number | null;
}

export type ModQuizCreateGradeItemPerSectionReturns = unknown;

export type ModQuizCreateGradeItemPerSectionReturn = ModQuizCreateGradeItemPerSectionReturns;
export type mod_quiz_create_grade_item_per_section_returns = ModQuizCreateGradeItemPerSectionReturns;
