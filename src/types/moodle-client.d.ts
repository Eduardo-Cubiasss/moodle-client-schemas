// Ambient forward declaration for @didactika/moodle-client to support declaration merging
// without requiring a circular dependency at compile time.
declare module '@didactika/moodle-client' {
    export interface MoodleClient {}
}
